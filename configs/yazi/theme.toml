# Xiu yazi theme. This whole file is written by wallcolors.py on every palette
# change (mgr, tabs, mode, filetype); yazi.toml next to it — keys, openers —
# is yours and never touched. Until the first wallpaper change yazi runs its
# defaults.
