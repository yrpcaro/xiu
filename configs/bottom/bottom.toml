# Xiu bottom (btm). The [styles] palette block below the header is written by
# wallcolors.py on every palette change; this file keeps your layout and flags.
# Until the first wallpaper change bottom runs its defaults.
