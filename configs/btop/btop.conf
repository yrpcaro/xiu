# Xiu btop. The theme file itself is written by wallcolors.py on every
# palette change (themes/xiu.theme); this file only points at it.
theme = "xiu"
theme_background = false
truecolor = true
rounded_corners = true
force_tty = false
presets = {"cpu": ["Default:cpu", "cpu:0"], "mem": ["Default:mem", "mem:0"]}
vim_keys = true
graph_symbol = "braille"
shown_boxes = "cpu mem net proc"
update_ms = 1500
